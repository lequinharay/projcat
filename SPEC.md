# SPEC: projcat (Project Concatenator)

## 1. 概要

`projcat` は、開発プロジェクトのソースコード、ディレクトリ構造、およびメタドキュメント（README等）を、構造化された1ファイルのテキストとして連結・出力するコマンドラインツール（CLI）である。

- **正式名称（バイナリ名）:** `projcat`
- **推奨エイリアス:** `pcat` （手元での高速なタイピング用）

---

## 2. 動作環境・技術スタック

- **開発言語:** Go (Golang)
- **想定シェル:** `zsh` (macOS上での動作を主想定とする)
- **外部依存:** ローカル環境に `git` コマンドがインストールされており、実行パスが通っていること。
  - _注意:_ 実行環境に `git` が存在しない、あるいは適用対象がGitリポジトリとして初期化されていない場合、ツールは適切なエラーメッセージを出力して終了コード非0で即座に異常終了（エラー処理）する。

---

## 3. 入出力仕様・フォーマット

### 3.1 出力形式

原則として、すべての出力は標準出力（`stdout`）、またはオプション指定によりクリップボードへ直接格納される。

#### Markdownネスト破壊（衝突）の完全回避規則

出力ファイルの最外枠は、常に4つ以上の連続するバックティック（````` `）で囲う。
FILESセクション等で展開される内部の各ファイルについて、そのファイルの実態テキスト内に含まれる最大連続バックティック数を走査し、外枠のバックティック数がそれを上回るように動的にエスケープ/ネスト制御を行う。

### 3.2 出力構造（レイアウト）

出力は以下のセクション順序で厳格に構成される。

1.  **# {projectName}**
    - カレントディレクトリ、または第1引数で指定されたパスからプロジェクト名を推定して表示する。
    - デフォルトの推定ロジックは `$HOME/github/username/{projectName}` の構造を基準とする。
    - `$HOME/github/username/` に相当するベースパス部分は、事前設定ファイル（`.env` 等）で変更可能とする。
2.  **## README / SPEC / ROADMAP **
    - これらをまとめて「特別な説明」と呼ぶ
    - プロジェクトルートに以下のファイルが存在するかチェックし、存在すれば最優先で配置する。
    - 優先順位（高い順）: `README.md` ＞ `SPEC.md` ＞ `ROADMAP.md`
    - ここで読み込まれたファイルは、後述の `FILES` セクションでは重複して展開されない（スキップされる）。
3.  **## PROMPT**
    - 事前設定ファイル（ `.projcatpromptfiles` ）で指定されたプロンプト用ファイル（例: `.clinerules`, `.cursorrules`）が存在すれば、その中身を展開する。
    - ここで読み込まれたファイルも、`FILES` セクションではスキップされる。
4.  **## DIR**
    - `tree` コマンド形式で、プロジェクトルートからの構造を表示する
5.  **## FILES**
    - `DIR` セクションで出力された順番に準拠し、対象となる全ファイルの中身を展開する。各ファイルは ` ```path/to/{filename.ext} \n {内容} ``` ` で囲われる。

```例
# {projectName}

## README
\`\`\`README.md
{内容}
\`\`\`

## SPEC
\`\`\`SPEC.md
{内容}
\`\`\`

## ROADMAP
\`\`\`ROADMAP.md
{内容}
\`\`\`

## PROMPT
\`\`\`.clinerules
{内容}
\`\`\`

\`\`\`.cursorrules
{内容}
\`\`\`

## DIR
src
├── a.ts
└── b
    ├── b-1.ts
    └── b-2.ts
\`\`\`

## FILES

\`\`\`{path/to/filename.ext}
{内容}
\`\`\`

・
・
・
```

---

## 4. 機能詳細・走査ロジック

### 4.1 ファイル走査とフィルタリング（DIR / FILES 共通）

- **シンボリックリンクの一律無視:**
  無限ループおよびプロジェクト外ファイルへのアクセス（暴走リスク）を完全に排除するため、シンボリックリンクは一切追跡せず、一律で無視・スキップする。
- **ドットファイルの省略:**
  `.` で始まるファイル（dotfile）は原則として省略する。ただし、「特別な説明」や「PROMPT」で明示的に指定されたファイルは例外とする。
- **Git連携による無視リストの適用:**
  既存の `.gitignore`、および本ツール専用の `.projcatignore`を参照する。
  - _実装ロジック:_ パフォーマンスを担保するため、走査した全ファイルパスのリストをメモリ上に構築し、`git check-ignore --stdin` に対して一括でパイプ流し込みを行うことで、無視すべきファイルを高速に一括判定する。
  - _実装ロジック:_ .projcatignore については `/*` が末尾に付けばディレクトリ、そうでなければファイルと判定する

---

## 5. CLI引数・オプション仕様

### 第1引数: `[target_directory]`

- 指定したディレクトリ以下のファイルを走査対象とする。
- カレントパスから推測したプロジェクト（リポジトリルート）の外側を誤って指定した場合は、エラーとして処理する。
- `README.md`, `SPEC.md`, `ROADMAP.md`, `.gitignore`, `.projcatignore` などのメタファイル、およびPROMPT設定ファイルは、この引数の指定にかかわらず、常にプロジェクトルート（リポジトリルート）に配置されている前提で固定参照する。
- DIRに表示する構造はここでの指定に関わらずプロジェクトルートからの内容を表示する

### オプション: `--filter=part_of_filename`

- ファイル名に対する部分一致フィルタリング。 `FILES` セクションにのみ影響する。
- カンマ区切り（`,`）で複数指定が可能。
  - OR条件として解釈する
  - ルートからの相対パスの何処かに該当の文字列が含まれていれば表示する

### オプション: `--clipboard`

- 本オプションが指定された場合、生成された全テキスト出力を、ファイルへの書き出しや標準出力への垂れ流しではなく、macOSのクリップボード（内部的に `pbcopy` への非同期パイプ処理）に直接格納する。

---

## 6. テスト・品質保証（QA基準）

開発時およびCI環境において、以下のテストケースを必須とし、堅牢性を検証する。

1. **正常系:** 各種オプション（`--filter`）が正確に動作し、期待通りのMarkdown（バックティック数が整合している状態）が出力されること。
2. **準正常系（Git未存在）:** `.git` がない、または `git` コマンドが使えない環境で、安全にエラー終了コードを返して落ちること（暴走・ハングアップの禁止）。
3. **境界値テスト（Limitの挙動）:** ファイルの境界線上、およびFILES以前で文字数が溢れるプロジェクト構造を擬似的に作成し、仕様通りの改頁制御・警告メッセージが出力されること。
4. **堅牢性テスト（無限ループ回避）:** ディレクトリ内に自己参照するシンボリックリンクを配置し、ツールがそれを完全に無視して正常終了すること。
